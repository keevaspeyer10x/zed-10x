# Zed 10x Test Plan Analysis

All five installed rows passed on the exact installed candidate. The main prior false-positive mechanisms were proxy substitution and isolated-route testing: direct ACP/provider canaries had been accepted as evidence for UI selectability, host labels, assembled launch wrappers, restart recovery, and transitions between individually green agents. The executed plan removes those inferences and binds every applicable External Agent surface × journey cell either to a direct installed observation or to a mechanically equivalent shared path with a current deterministic regression.

The highest-risk journey—persistent-session recovery after a real service death—passed across the assembled boundary. The installed Zed 10x app observed a finite transport failure after the exact Intrepid lane service was restarted, Retry reattached through a fresh InvocationID, and the same session history and project returned without prompt replay. Switch-and-return, ordinary cleanup, persistent detach semantics, and terminal/task/Vim/MCP/DAP journeys also passed. The predecessor `fb1fb767` full route matrix is retained only as historical direct-route support; current acceptance is bound to the `a6531bd9` installed journey and current deterministic regressions. The remaining release risk is limited to rebuilding the protected merged revision, reinstalling it on both hosts, and running the post-merge consumer canary.
