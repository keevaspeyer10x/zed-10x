# Zed 10x Test Plan Analysis

All five installed rows passed on the exact installed candidate. The main prior false-positive mechanisms were proxy substitution, isolated-route testing, and over-broad equivalence: one provider had been allowed to stand in for routes with different commands, settings, authentication, and provider behavior. The corrected plan requires a direct current receipt for every visible route's inventory, launch outcome, and cleanup. Switch-and-return and persistent recovery remain explicitly surface-level state journeys on named representatives; they are no longer projected as per-provider results.

The highest-risk journey—persistent-session recovery after a real service death—passed across the assembled boundary. The installed Zed 10x app observed a finite transport failure after the exact Intrepid lane service was restarted, Retry reattached through a fresh InvocationID, and the same session history and project returned without prompt replay. Switch-and-return, ordinary cleanup, persistent detach semantics, and terminal/task/Vim/MCP/DAP journeys also passed.

Current route acceptance is bound to the exact installed `2bde4bdf` Mac and Intrepid runtimes. All eight Mac routes passed. Fifteen Intrepid routes passed; Claude Intrepid secondary produced one content-free response without project evidence, then passed its one ordinary bounded retry, while Claude Intrepid work was correctly isolated as expired external authentication. Every route cleaned up, no product failure remains, and no result is projected from one provider or profile to another. The assembled UI journey at `a6531bd9` remains valid ancestor evidence because its load-bearing UI bytes are unchanged and are hash-bound by the lifecycle validator. The remaining release risk is limited to rebuilding the protected merged revision, reinstalling it on both hosts, and running the post-merge consumer canary.
