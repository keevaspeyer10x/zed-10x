# Zed 10x Test Plan Analysis

All five installed rows are implemented and green. The main prior false-positive mechanisms were proxy substitution and isolated-route testing: direct ACP/provider canaries were accepted as evidence for UI selectability, host labels, assembled launch wrappers, restart recovery, and transitions between individually green agents. The completed plan removes those inferences and binds each independently visible External Agent variant to every applicable user journey, including explicit switch-and-return and restart transitions.

The real assembled boundaries now cover both Intrepid ownership models. Persistent recovery retained its original session and journal, while the exact installed a4a75c9 app survived an ordinary remote-server restart, retired the superseded dedicated agent transport, created exactly one replacement, and cleaned all canary-owned processes on close. The remaining release residual is to repeat the build, install, and canary from the exact protected merged revision.
